package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Frosted Glass Theme Design Colors (matching the HTML design specification)
val BoeingBlue = Color(0xFF0039A6)     // Frosted blue brand accent color (#0039A6)
val ElectricBlue = Color(0xFF00D4FF)   // Cyan/electric blue highlights
val CyberCyan = Color(0xFF00FFCC)      // Cyber/adventure green-cyan accent
val DarkSlate = Color(0xFF0B0D11)      // Base deep slate body background (#0B0D11)
val CardBackground = Color(0x991C2028) // Translucent glass card background (60% of #1C2028)
val SecondaryText = Color(0xFF94A3B8)  // Medium slate gray descriptions
val BorderColor = Color(0x0EFFFFFF)    // Translucent glass white border (5.5% opacity)
val SuccessGreen = Color(0xFF4ADE80)   // Optimal status / locked green
val WarnOrange = Color(0xFFFB923C)     // Warm orange highlights
val BatteryGreen = Color(0xFF4ADE80)   // High performance green
val DarkRed = Color(0xFFEF4444)        // Emergency termination color

